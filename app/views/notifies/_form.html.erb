<%= form_with(model: notify, local: true) do |form| %>
  <% if notify.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notify.errors.count, "error") %> prohibited this notify from being saved:</h2>

      <ul>
      <% notify.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :notify_name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :notify_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
